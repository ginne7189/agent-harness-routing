# 이 저장소를 읽는 순서

```text
AGENTS.md
→ 전체 목표·공통 작업 원칙·금지 행동

workflow.md
→ 이 패턴의 실행 순서·분기·반복·종료 조건

roles/
→ 각 역할이 받는 입력과 만들어야 하는 결과

contracts/
→ 단계 또는 역할 사이에 전달하는 값의 형식

policies/
→ 자동 실행·사람 확인·중단 기준

tools/
→ Local Tool·MCP Server·MCP Tool과 권한 구분

.agents/skills/
→ Codex가 이 업무를 처리할 때 따라야 할 순서
```

이 저장소에는 Python·JavaScript 실행 코드가 없습니다. 파일을 열어 **Workflow 패턴이 Harness 설계로 어떻게 표현되는지** 비교하는 개념 학습용 예시입니다.

